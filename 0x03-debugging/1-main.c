# incluye  < stdio.h >

/ * *
 * main - provoca un bucle infinito
 * Devolución: 0
 * /

int  main ( vacío )
{
	int i;

	printf ( " Bucle infinito entrante :( \ n " );
	/ * *
	 * i = 0;
	 *
	 * mientras (i <10)
	 * {
	 * putchar (i);
	 *}
	 * /
	printf ( "¡ Bucle infinito evitado! \\ o / \ n " );

	return ( 0 );
}
